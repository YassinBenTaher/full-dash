import { HoverDropDownDirective } from './hover-drop-down.directive';

describe('HoverDropDownDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverDropDownDirective();
    expect(directive).toBeTruthy();
  });
});
